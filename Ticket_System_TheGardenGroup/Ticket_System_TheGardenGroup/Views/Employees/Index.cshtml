@using Ticket_System_TheGardenGroup.ViewModels;
@model Task<List<EmployeeTicketsVm>>
@{
    ViewData["Title"] = "Employees";
}

<div class="text-center">
    <h1>User Management</h1>
    <a href="/Employees/AddEmployee" class="btn btn-primary">Create Ticket</a>

    <table class="table table-info table-hover">
        <partial name="_EmployeesHeader" />
        <tbody>
            @foreach(EmployeeTicketsVm employeeTicketsViewModel in await Model)
            {
                <tr>
                    <td>@employeeTicketsViewModel.EmployeeNumber</td>
                    <td>@employeeTicketsViewModel.EmailAddress</td>
                    <td>@employeeTicketsViewModel.Name</td>
                    <td>@employeeTicketsViewModel.Surname</td>
                    <td>@employeeTicketsViewModel.EmployeeRole</td>
                    <td>@employeeTicketsViewModel.TotalTickets</td>
                    <td>
                        <a href="/Employees/ViewEmployee/@employeeTicketsViewModel.EmployeeNumber">View</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

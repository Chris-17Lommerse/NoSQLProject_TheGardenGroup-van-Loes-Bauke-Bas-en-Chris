@model List<Ticket>

@{
    ViewData["Title"] = "Archived tickets";
}

<h1 class="alert-danger">@TempData["ErrorMessage"]</h1>

<h1>Archived Tickets</h1>
<a href="/Tickets/index" class="btn btn-primary">Back to ticket list</a>

<table class="table table-info tavle-hover">
    <partial name="_TicketsHeader" />
    <tbody>
        @foreach (Ticket ticket in Model)
        {
            <tr>
                <td>@ticket.TicketId</td>
                <td>@ticket.CreationTime</td>
                <td>@ticket.TicketName</td>
                <td>@ticket.Description</td>
                <td>@ticket.TicketStatus</td>
                <td>@ticket.ReportingEmployee.EmailAddress</td>
                <td>@ticket.SolvingEmployee.EmailAddress</td>
                <td>@ticket.IsSolved</td>
                <td>
                    <a href="/Tickets/UpdateTicket?ticketId=@ticket.TicketId">View</a>
                </td>
            </tr>
        }
    </tbody>
</table>